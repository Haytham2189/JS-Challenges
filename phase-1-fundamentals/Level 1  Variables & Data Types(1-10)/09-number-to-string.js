//Number to String  Store a number and convert it to a string using .toString() then check its type. typeof 42 → number then typeof "42" → string//
const number=21
const text=number.toString()

console.log(typeof text);
console.log(typeof number);
